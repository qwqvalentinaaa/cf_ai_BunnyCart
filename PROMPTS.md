- In which situations should I use a Workflow, a Worker, or a Durable Object for coordinating my shopping assistant logic?
- How do I integrate a workflow as a tool so the LLM knows when to call it in my AI shopping assistant?
- How to make LLM rank products in my workflow?
- After the workflow returns top products, do I need a separate tool to generate pros and cons, or can the AI handle it automatically?
- How should I word the sample questions in the UI to suggest user queries including budget?
- How does env and wrangler.jsonc work?

